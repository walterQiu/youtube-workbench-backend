from django.apps import AppConfig


class OrganizerConfig(AppConfig):
    """Application configuration for the organizer app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "organizer"
